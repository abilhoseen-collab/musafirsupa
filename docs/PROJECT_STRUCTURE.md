# 📁 প্রজেক্ট স্ট্রাকচার

সম্পূর্ণ প্রজেক্ট অর্গানাইজেশন এবং ফাইল ব্যবস্থাপনা।

## 📂 রুট ডিরেক্টরি

```
air-travel-agency-management-system/
├── backend/                    # Node.js + Express ব্যাকএন্ড
├── frontend/                   # React ফ্রন্টএন্ড
├── database/                   # ডাটাবেস স্কিমা
├── docs/                       # ডকুমেন্টেশন
├── scripts/                    # হেল্পার স্ক্রিপ্ট
├── docker-compose.yml          # ডকার কম্পোজ
├── .gitignore                  # গিট ইগনোর
├── README.md                   # প্রধান ডকুমেন্ট
└── LICENSE                     # লাইসেন্স
```

## 📂 ব্যাকএন্ড স্ট্রাকচার

```
backend/
├── src/
│   ├── app.js                  # এক্সপ্রেস অ্যাপ
│   ├── config/
│   │   ├── database.js         # ডাটাবেস সংযোগ
│   │   └── constants.js        # ধ্রুবকগুলি
│   ├── controllers/
│   │   ├── authController.js
│   │   ├── ticketController.js
│   │   ├── bookingController.js
│   │   ├── umrahController.js
│   │   └── ...
│   ├── middleware/
│   │   ├── auth.js             # JWT অথেনটিকেশন
│   │   └── errorHandler.js     # এরর হ্যান্ডলিং
│   ├── routes/
│   │   ├── authRoutes.js
│   │   ├── ticketRoutes.js
│   │   ├── bookingRoutes.js
│   │   ├── umrahRoutes.js
│   │   ├── paymentRoutes.js
│   │   ├── customerRoutes.js
│   │   ├── userRoutes.js
│   │   └── reportRoutes.js
│   ├── utils/
│   │   ├── validators.js       # ভ্যালিডেশন স্কিমা
│   │   ├── logger.js           # লগিং
│   │   └── helpers.js          # হেল্পার ফাংশন
│   └── __tests__/              # টেস্ট ফাইল
├── scripts/
│   ├── seed-database.js
│   └── backup-database.sh
├── logs/                       # লগ ফাইল
├── server.js                   # সার্ভার এন্ট্রি পয়েন্ট
├── package.json
├── .env.example
├── Dockerfile
└── .gitignore
```

## 📂 ফ্রন্টএন্ড স্ট্রাকচার

```
frontend/
├── src/
│   ├── App.js                  # মেইন অ্যাপ
│   ├── index.js                # এন্ট্রি পয়েন্ট
│   ├── components/
│   │   ├── Sidebar.js          # সাইডবার নেভিগেশন
│   │   ├── Navbar.js           # টপ নেভিগেশন
│   │   ├── Table.js            # রিইউজেবল টেবিল
│   │   ├── Modal.js            # রিইউজেবল মডাল
│   │   └── Form.js             # রিইউজেবল ফর্ম
│   ├── pages/
│   │   ├── LoginPage.js
│   │   ├── DashboardPage.js
│   │   ├── TicketsPage.js
│   │   ├── BookingsPage.js
│   │   ├── UmrahPage.js
│   │   ├── PaymentsPage.js
│   │   ├── CustomersPage.js
│   │   └── ReportsPage.js
│   ├── context/
│   │   └── authStore.js        # অথ স্টেট ম্যানেজমেন্ট
│   ├── services/
│   │   └── api.js              # API কল সেন্ট্রালাইজেশন
│   ├── styles/
│   │   └── index.css           # গ্লোবাল স্টাইল
│   ├── utils/
│   │   └── formatters.js       # ডেটা ফরম্যাটিং
│   └── __tests__/              # টেস্ট ফাইল
├── public/
│   └── index.html
├── package.json
├── .env.example
├── Dockerfile
├── nginx.conf
└── .gitignore
```

## 📂 ডাটাবেস স্ট্রাকচার

```
database/
├── schema.sql                  # সম্পূর্ণ ডাটাবেস স্কিমা
├── migrations/                 # মাইগ্রেশন ফাইল
├── seeds/                      # সিড ডেটা
└── backups/                    # ব্যাকআপ ফাইল
```

## 📚 কী ফাইল বর্ণনা

### ব্যাকএন্ড কী ফাইল

| ফাইল | উদ্দেশ্য |
|------|---------|
| `server.js` | সার্ভার এন্ট্রি পয়েন্ট |
| `app.js` | এক্সপ্রেস অ্যাপ কনফিগারেশন |
| `authController.js` | লগইন/রেজিস্ট্রেশন লজিক |
| `ticketController.js` | টিকেট ম্যানেজমেন্ট লজিক |
| `database.js` | PostgreSQL সংযোগ |
| `auth.js` | JWT মিডলওয়্যার |

### ফ্রন্টএন্ড কী ফাইল

| ফাইল | উদ্দেশ্য |
|------|---------|
| `index.js` | React অ্যাপ এন্ট্রি |
| `App.js` | মেইন রাউটিং |
| `authStore.js` | গ্লোবাল অথ স্টেট |
| `api.js` | Axios ইনস্ট্যান্স |
| `LoginPage.js` | লগইন পেজ |
| `DashboardPage.js` | ড্যাশবোর্ড পেজ |
