# 👨‍💻 ডেভেলপমেন্ট গাইড

এই ডকুমেন্টে প্রজেক্ট ডেভেলপমেন্টের জন্য দিকনির্দেশনা রয়েছে।

## 🔧 ডেভেলপমেন্ট সেটআপ

### প্রয়োজনীয়তা
- Node.js 18+
- PostgreSQL 12+
- Git

### ইনস্টলেশন স্টেপস

1. রেপোজিটরি ক্লোন করুন:
```bash
git clone https://github.com/abilhoseen-collab/air-travel-agency-management-system.git
cd air-travel-agency-management-system
```

2. ব্যাকএন্ড সেটআপ:
```bash
cd backend
npm install
cp .env.example .env
# .env ফাইল সম্পাদনা করুন
npm run dev
```

3. ফ্রন্টএন্ড সেটআপ (নতুন টার্মিনাল):
```bash
cd frontend
npm install
cp .env.example .env
npm start
```

## 📝 কোডিং স্ট্যান্ডার্ড

### ফাইল নামকরণ
- ফাংশন: `camelCase` (e.g., `getUserProfile()`)
- ফাইল: `kebab-case` (e.g., `user-controller.js`)
- কনস্ট্যান্ট: `UPPER_SNAKE_CASE` (e.g., `MAX_FILE_SIZE`)

### কমেন্টিং
```javascript
/**
 * ফাংশনের সংক্ষিপ্ত বর্ণনা
 * @param {type} paramName - প্যারামিটার বর্ণনা
 * @returns {type} রিটার্ন বর্ণনা
 */
function functionName(paramName) {
  // কোড
}
```

### এরর হ্যান্ডলিং
```javascript
try {
  // কোড
} catch (error) {
  logger.error('সংক্ষিপ্ত বর্ণনা', error, { additionalData });
  res.status(500).json({ error: 'ব্যবহারকারী-বান্ধব বার্তা' });
}
```

## 🧪 টেস্টিং

### ইউনিট টেস্ট চালান
```bash
cd backend
npm test
```

### API টেস্ট (Postman/cURL)
```bash
# লগইন
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"password123"}'
```

## 🚀 ডিপ্লয়মেন্ট

### ডকার দিয়ে
```bash
docker-compose up -d
```

### ম্যানুয়াল ডিপ্লয়মেন্ট
```bash
# ব্যাকএন্ড
cd backend && npm install && NODE_ENV=production npm start

# ফ্রন্টএন্ড
cd frontend && npm install && npm run build
# build ফোল্ডার কোথাও সার্ভ করুন
```

## 📚 দরকারী কমান্ডস

```bash
# ডাটাবেস রিসেট
psql air_travel_agency < database/schema.sql

# লগ দেখুন
tail -f logs/app.log

# নির্ভরতা আপডেট করুন
npm update

# নিরাপত্তা অডিট
npm audit
```

## 🐛 ডিবাগিং

### ব্যাকএন্ড ডিবাগিং
```javascript
console.log('ডিবাগ মেসেজ:', variable);
// বা
debugger;
```

### ফ্রন্টএন্ড ডিবাগিং
- Chrome DevTools (F12)
- React DevTools এক্সটেনশন

## 📦 ডিপেন্ডেন্সি আপডেট

```bash
# সেফ আপডেট
npm update

# প্যাচ আপডেট
npm upgrade --save
```
